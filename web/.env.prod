# Production defaults — same-origin (Gateway serves web/dist/). basePath comes from
# gateway.controlUi.basePath at runtime via the inline boot script in index.html.
VITE_APP_ENV="prod"
VITE_GATEWAY_HTTP_URL=""
VITE_BASE_PATH="./"
