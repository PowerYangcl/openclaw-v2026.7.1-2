# Local dev defaults — Gateway expected at 127.0.0.1:18789 (Vite proxy is set up in vite.config.ts).
# Per-tab session/pairing state lives in sessionStorage; no persistent credentials in this file.
VITE_APP_ENV="dev"
VITE_GATEWAY_HTTP_URL=""
VITE_BASE_PATH="./"
